{
    "defaultConnection":"default",
    "connections":{
        "default":{
            "connector":"bookshelf",
            "settings":{
                "client":"mysql",
                "host":"${process.env.INSTANCE_CONNECTION_NAME}",
                "port":"${process.env.INSTANCE_}"
            }
        }
    }
}